============
Installation
============

At the command line::

    $ easy_install django-fwdform

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-fwdform
    $ pip install django-fwdform
